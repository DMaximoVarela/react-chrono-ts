import Layout from "@Components/Layout";
import { Title } from "@react-chrono-ts/ui";

const Landing = () => {
  return (
    <Layout>
      <div>
        <Title text="Landing" />
      </div>
    </Layout>
  );
};

export default Landing;
